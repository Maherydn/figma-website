import { FC } from "react";

const HeroSubtitle: FC = () => (
  <p className="text-slate-500 font-semibold lg:text-lg text-sm max-w-xl">
    Supercharge your design workflow with top Figma & Design System techniques.
    Powerful components with tokens to complete every project 10× faster.
  </p>
);

export default HeroSubtitle;
